
// A set of message headers.
ushort          num_attributes;
header          [num_attributes] {
  // Header code (specific to the type of the Message).
  ushort          code;

  // Header data.
  string           value;
}

ubyte cardinality;

// Argument data descriptor ID.
byte[16] input_typedesc_id;

// Result data descriptor ID.
byte[16] output_typedesc_id;
